import RootLayout from "@/layout";
import { Container } from "@mui/material";

const Settings = () => {
    return (
        <RootLayout pageTitle="Settings" pageDescription="Manage your settings">
            <Container>
                <h1>Settings</h1>
            </Container>
        </RootLayout>
    );
}

export default Settings;